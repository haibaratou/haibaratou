# 配信リポジトリの役割

このリポジトリは、サイトとゲームを配信する実行先です。公開／非公開による
データ区分は行わず、`etymon-source` の同じ正本から生成します。

## 原則

- 語源データの正本は `etymon-source` です。
- ゲームは `etymon-game-lab` で開発・検査してから反映します。
- 外部チームを含む各利用先は同じ生成済みデータ形式を使います。

## 実行用語源データ

ゲームは `app/data/generated-etymon/**` を読みます。このフォルダは手編集せず、
母艦の `etymon-source/app/export_public_game_data.py` で更新します。これは権限分離ではなく、
全実行先で同じ形式を保証するための生成物です。
