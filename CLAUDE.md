# CLAUDE.md — haibaratou/haibaratou プロジェクト引き継ぎ

> **まず `README-AIのための取扱説明書.md` を読むこと。**
> リポジトリの構成・push の道すじ・語源データベース(英語/日本語)の
> 作りかたと、やってはいけないことが全部そこにある。

このリポジトリは、印欧語根(PIE)をテーマにした2つの連動プロダクトを開発している。
以前はブラウザチャット版のClaude(サンドボックス環境)で開発していたが、
毎回ダウンロード→手動配置が必要で非効率だったため、**Claude Codeに移行した**。
Claude Codeはこのローカルリポジトリのファイルとgitを直接操作できるので、
今後は「修正 → その場でcommit → push」まで一気通貫で行う。

---

## 重要な前提(最初に必ず読むこと)

- **`app/lexicopia-hakoniwa.html` は Nanbara が Codex で修正した最新版がリポジトリにある。**
  以前のClaude(サンドボックス版)が持っている版は古い。**この本体を勝手に上書き・巻き戻ししないこと。**
  変更するときは、必ず現在リポジトリにある `app/lexicopia-hakoniwa.html` を起点に diff で編集する。
  (参考用に、旧Claudeが作った v7 系のスナップショットを `app/lexicopia-hakoniwa-v7-mine.html`
   として同梱してあるが、これは**参照専用**。リポジトリ本体とは別物として扱う。)

- 反映先は **`app/` フォルダ**。GitHub上の正規パスは以下の3つ:
  - `app/index.html` … 下の2つへのハイパーリンクだけを持つ入口ページ
  - `app/lexicopia-hakoniwa.html` … ゲーム本体(Codex修正済みの最新がリポジトリにある)
  - `app/etymopedia.html` … 共創型オンライン語源辞典(単一HTML)

- 旧環境ではgitのPATにContents書き込み権限が無く push が常に 403 だった。
  Claude Code では、ローカルで一度 git 認証を通してあれば push できるはず。
  最初に `git remote -v` と、小さな変更での push 疎通を確認すること。

---

## プロダクト1: エティモリンゴ:レキシコピア(ゲーム / `app/lexicopia-hakoniwa.html`)

PIE語根を「ワイルド」というキャラにした箱庭+遠征ゲーム。単一HTML、CSV駆動、
`assets/` の画像を使う。**最新はリポジトリの版(Codex修正済み)を正**とする。

8つの語根クラン(色):
kaput(頭/#e8875a), ane(息をする/#43b5a0), bha(話す/#f2a444), gwei(生きる/#7cba62),
oino(ひとつ/#7f8ff0), reg(まっすぐ/#5aa7e8), weid(見る・知る/#a06ee0), ye(投げる/#f26d99)

旧Claudeが実装した主な機能(v7時点。リポジトリ版で変わっている可能性あり、要現物確認):
- 起動時に設定モード(箱庭に出すキャラを絵だけのタイルで選択)
- 箱庭: キャラがうろつく(activityで個体差)、コリジョン、タップでズーム+生産、
  ドラッグ移動、生産タイマーは頭上ピル(左右反転しない独立要素)、完成品タップで倉庫へ
- 遠征: 4匹編成(1クラン1匹)+8枠アイテム(初期選択済み)、背景スクロールで走行、
  英日ログ読み上げ(初期OFF)、最低3分、放置対応(拠点へ戻る/残り時間表示/オフライン加算最大60分)、
  クリアはCongratulations演出+ログ完結(別窓なし)、定期的に影の軍団と自動戦闘
- おえかき: キャラ選択→下絵をなぞってペン/消しゴム/スポイト/バケツ、太さスライダー、
  抽出パレット+カラーピッカー+最近色、undo/redo、図鑑登録。
  キャラ画像はbase64で埋め込み済みなのでfile://でもcanvas登録が完結する
- エティモン図鑑: 語源解説つきの図鑑。描いた絵が各所のキャラ絵を差し替える
- データCSVは `app/data/`(config/images/wilds/items/recipes/areas/enemies/etymon)
- file://直開きだとCSVが読めず内蔵データで動く。CSV反映は http 経由
  (`python -m http.server 8000` → `http://localhost:8000/app/lexicopia-hakoniwa.html`)

---

## プロダクト2: エティモペディア(語源辞典 / `app/etymopedia.html`)

ゲームの「描いて登録」体験を、ゲーム外の公開語源辞典に流し込む共創サイト。
クライアント提示用の**映像サンプル(モック)**。研究者向けの堅い辞書ではなく、
クリエイターや若年層を巻き込むポップな見た目にしてある。**この版が現時点の最新。**

### 設計方針(Nanbaraの指示で確定した重要ルール)
- **「印欧語根」という語は使わない。**「語根」または「祖先」と呼ぶ。
- **「タネ(種)」という植物比喩は使わない。** ことばの「祖先」(動物)と表現する。
- **語根ページの家系図は「キャラ中心の放射状マップ」**(2026-07 更新。旧ルールの縦一列は廃止)。
  語根キャラを中央に置き、そこから生まれた英単語を**左右にバランスよく5個以内**で放射状に配置。
  連結線は**キャラ→各単語だけ**を引き、**単語どうしは絶対つながない**(時系列誤解を防ぐため)。
  印欧祖語(`*ane-` 等)は小さく添える程度にし、**概念(「息をする」等)を主役**にする。
  さらに**ちょっとした語源の蘊蓄メモ**(roots.csv の note)を1枚出す。
- **各単語には語派(地域)バッジを出す**:ゲルマン / ロマンス / ギリシャ。
  **旧「ラテン」表記は「ロマンス」に統一**(内部コード L は表示上「ロマンス」)。
  単語の図版が無い場合は「募集中」ではなく**仮の画像(語根キャラ)**を出す(あとで CSV で差し替え)。
- **派生語辞典(下部の一覧)にも地域タグ(via)を残す**(G=ゲルマン / L=ロマンス / Gr=ギリシャ)。
- **単語ずかんと図版ギャラリーは統合済み**(1セクション)。分けない。
- 単語ページに、余計な煽り/説明文を入れない。
  「応募N枚」「いま人気の図版」「あなたも描いて応募」等の文言は入れない。
  ゲーム外からの投稿・編集はできない前提なので、投稿を促すUIは出さない。
- ヒーロー下のスクロール帯は、語根キャラのアイコン+意味(例: kaput→頭)を
  ゆっくり流し(90s)、タップでその語根ページへ。

### データはすべてCSV駆動(`app/data/`)
文言も画像パスも全部CSVで編集できる(2026-07 に埋め込みから実ファイルへ外出し済み)。

**2026-08 から、語根・単語データは語源データベース(旧称エティモン探索)(`app/data/pie/`)を上流とする。**
`python3 app/build_etymopedia_from_pie.py` が roots.csv(蘊蓄)/words.csv(レア度・語派)/
lexicon.csv(派生語辞典。レア度999以下を全部収録、noteは語源メモ)を再生成する。
代表4語(キャラ周りの放射状マップ)は `featured.csv`(root,w)で指定し、
差し替えたらこのスクリプトを再実行するだけでよい(語義・レア度・語派・語源メモが自動で付く)。
レア度は語源データベースの r 値(1〜999)を持ち、画面では★5段階
(まいにち語/よくみる語/ときどき語/レア語/げきレア語)で表示する。CSVファイル:
- `ui.csv` … サイト全体の文言(ヒーロー/見出し/フッター等)
- `roots.csv` … 語根(id/root/jp/color/art/note)。art列に語根キャラ画像パス。note列が蘊蓄メモ
- `morphs.csv` … 単語分解の断片(接頭辞など)。root列があれば語根扱い
- `words.csv` … 図版つき単語ページ本体(分解/IPA/品詞/語義/例文/語源メモ)。**region列=語派**(G/L/Gr)
- `lexicon.csv` … 派生語辞典。**via列が地域タグ**(G/L/Gr/Sa/Ce/Sl。L は表示上「ロマンス」)。**r列=レア度**
- `featured.csv` … 語根ごとの代表4語(放射状マップに出す語)。編集して build_etymopedia_from_pie.py を再実行
- `submissions.csv` … 各単語の応募図版(複数可)。img列に画像パス、by/likes

**CSVを編集したら `python3 app/build_etymopedia_emb.py` を実行**して、HTML内の埋め込みCSV
(`EMB_CSV`、file://フォールバック用)を再生成すること。画像埋め込み(`EMB_IMG`)は触らない。

### 画像とCSVの読み込み挙動(重要)
`app/etymopedia.html` は単一HTMLだが、中に **CSVと画像のフォールバックを埋め込んである**。
- **http経由で開くと** `app/data/*.csv` を fetch して読む(CSV編集が反映される)。
  画像は `roots.csv`/`submissions.csv` のパス(例 `chara/...`, `seed/...`)を
  `../assets/` 基準で解決 or 埋め込みdataURLを使う。
- **file://直開きだと** fetchが失敗するので、HTML内の埋め込みCSV+埋め込み画像で表示する。
  → つまり**ダブルクリックでも中身が見える**。CSVを編集して反映したいときだけ http で開く。

この二重構造のため、**CSVを直したら `app/etymopedia.html` 内の埋め込みCSVも更新する必要がある**
(でないと file:// 時に古いまま見える)。旧環境ではPythonのビルドスクリプトで
「CSV+画像をHTMLに注入」して単一ファイルを生成していた。Claude Codeでも同様に、
`app/data/*.csv` を正としてHTMLの `EMB_CSV` / `EMB_IMG` を再生成する仕組みを組むとよい。
(ソーステンプレートは旧環境の `etymopedia_v3.html.tpl` にある。必要なら再構築する。)

収録内容: 12語根 × 代表4語 + 図版つき単語(計62語) + 派生語辞典 約490語(語源データベース由来)。
シード挿絵はゲーム素材の流用(captain=海賊ねこ 等、後で正式画像に差し替え前提)。

---

## 最初にやること(Claude Codeでの初手)

1. `git remote -v` / `git status` / `git log --oneline -5` で状態確認。
2. `app/` の3ファイルの現物を読む。特に **`app/lexicopia-hakoniwa.html` はリポジトリ版が正**。
   同梱の `app/lexicopia-hakoniwa-v7-mine.html` は旧Claudeの参考版なので混同しない。
3. `app/etymopedia.html` と `app/data/*.csv` は同梱の最新版で置き換え済み(このフォルダの内容)。
   内容を確認し、上のルールに沿っているか点検。
4. 小さな無害変更(コメント追加など)を1つ commit & push して、**push疎通を確認**。
   403が出るならローカルのgit認証(PATのContents権限)を見直す。
5. 以降は Nanbara の指示に応じて `app/` 内を編集し、その都度 commit & push する。


